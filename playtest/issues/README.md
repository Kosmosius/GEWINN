# Issue register

Store one issue file per reproducible rules defect or explicitly identified
ambiguity. Use `playtest/templates/rules-issue.md`; name files with a stable ID
such as `PT-001-short-title.md`. This directory intentionally contains no
actual findings at bootstrap.
